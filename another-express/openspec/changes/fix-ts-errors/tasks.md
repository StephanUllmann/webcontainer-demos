## 1. Fix in Author Routes

- [x] 1.1 Type Casting für `req.params.id` in `GET /authors/:id` Handler hinzufügen
- [x] 1.2 Type Casting für `req.params.id` in `PUT /authors/:id` Handler hinzufügen
- [x] 1.3 Type Casting für `req.params.id` in `DELETE /authors/:id` Handler hinzufügen

## 2. Fix in Book Routes

- [x] 2.1 Type Casting für `req.params.id` in `GET /books/:id` Handler hinzufügen
- [x] 2.2 Type Casting für `req.params.id` in `PUT /books/:id` Handler hinzufügen
- [x] 2.3 Type Casting für `req.params.id` in `DELETE /books/:id` Handler hinzufügen

## 3. Verifizierung

- [x] 3.1 `npx tsc --noEmit` ausführen und bestätigen, dass keine Fehler mehr auftreten
